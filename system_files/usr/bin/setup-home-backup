#!/usr/bin/bash
set -euo pipefail
echo "Existing home-borgmatic configurations and timers are unchanged."
echo "Opening optional named backup enrollment; no RAID or encryption setup is performed."
exec /usr/bin/setup-backup "$@"
